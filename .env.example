# Copy to apps/mobile/.env and fill in. EXPO_PUBLIC_* values are inlined into the app bundle.
# Use the SAME Clerk instance as the web app.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
